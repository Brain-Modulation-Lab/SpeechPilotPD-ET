electrodeLabels = labelIncludedElectrodes(includeCell);
electrodeLabels = {}; n=1; gp = groups{gg};
        for ss=1:includeCell
            inc = find(includeCell{ss});
            if ~isempty(inc)
                for ii=1:length(inc)
                    electrodeLabels{n} = sprintf('S%dE%d', ss, inc(ii));
                    n = n+1;
                end
            end
        end

